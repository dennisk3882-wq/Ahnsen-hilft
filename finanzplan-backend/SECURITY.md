# Security boundary

The static PWA may contain only public configuration such as the Supabase project URL and publishable key. Service-role, banking private keys, AI API keys, VAPID private key and dispatcher/state secrets are server-side only.
