'use strict';
(function(){
  const P=()=>FinanzPremium;
  function priorityTag(p){return `<span class="tag ${p==='high'?'red':p==='medium'?'amber':'green'}">${p==='high'?'Wichtig':p==='medium'?'Prüfen':'OK'}</span>`}
  function autopilotCard(){const a=P().autopilot(),f=a.forecast;return `<article id="premiumAutopilot" class="card premium-autopilot"><div class="card-title-row"><div><h2>Autopilot & Prognoseband</h2><p>Robuste Prognose aus Historie, aktuellem Ausgabetempo und festen Verpflichtungen</p></div><button id="openPremiumAutopilot" class="secondary-button">Details</button></div><div class="four"><div class="kpi-small"><small>Konservatives Monatsende</small><strong class="money ${f.monthEnd.conservative>=0?'positive':'negative'}">${money(f.monthEnd.conservative)}</strong></div><div class="kpi-small"><small>Basisszenario</small><strong class="money">${money(f.monthEnd.base)}</strong></div><div class="kpi-small"><small>Optimistisches Szenario</small><strong class="money">${money(f.monthEnd.optimistic)}</strong></div><div class="kpi-small"><small>Auffälligkeiten</small><strong>${a.alerts.length}</strong><small>Konfidenz ${f.confidence}</small></div></div></article>`}
  const baseDashboard=renderDashboard;renderDashboard=function(){baseDashboard();const root=$('#view-dashboard'),anchor=$('#moneyIntelligence',root)||$('.forecast-banner',root);if(anchor&&!$('#premiumAutopilot',root))anchor.insertAdjacentHTML('afterend',autopilotCard());$('#openPremiumAutopilot',root)?.addEventListener('click',openAutopilot)};
  function openAutopilot(){const a=P().autopilot(),f=a.forecast;openModal('Finanz-Autopilot','Die App rechnet lokal. Hinweise sind nachvollziehbar und verändern keine Buchungen automatisch.',`<div class="three"><div class="kpi-small"><small>Frei verfügbar</small><strong class="money">${money(a.safe.safe)}</strong></div><div class="kpi-small"><small>Variable Ausgaben erwartet</small><strong class="money">${money(f.variable.base)}</strong></div><div class="kpi-small"><small>Prognoseband Monatsende</small><strong class="money">${money(f.monthEnd.conservative)} – ${money(f.monthEnd.optimistic)}</strong></div></div><h3 style="margin-top:18px">Empfohlene Prüfungen</h3><div class="simple-list">${a.actions.map(x=>`<div class="list-row"><div>${priorityTag(x.priority)}</div><div><strong>${escapeHTML(x.title)}</strong><small>${escapeHTML(x.detail)}</small></div></div>`).join('')}</div><h3 style="margin-top:18px">Anomalien & Dubletten</h3><div class="simple-list">${a.alerts.length?a.alerts.map(x=>`<div class="list-row"><div>${priorityTag(x.severity)}</div><div><strong>${escapeHTML(x.title)}</strong><small>${x.type==='duplicate'?'Mögliche Doppelbuchung':`Betrag ${money(x.current)} statt typisch ${money(x.baseline)}`}</small></div><div>${fmtDate(x.date)}</div></div>`).join(''):'<div class="empty">Keine auffälligen Buchungen erkannt.</div>'}</div><h3 style="margin-top:18px">Verträge prüfen</h3><div class="simple-list">${a.contracts.length?a.contracts.map(x=>`<div class="list-row"><div>${priorityTag(x.priority)}</div><div><strong>${escapeHTML(x.title)}</strong><small>${escapeHTML(x.detail)}</small></div></div>`).join(''):'<div class="empty">Aktuell keine Vertragsprüfung erforderlich.</div>'}</div><div class="modal-actions"><button class="secondary-button" data-cancel>Schließen</button></div>`,()=>{$('[data-cancel]').onclick=closeModal})}

  const baseMore=renderMore;renderMore=function(){baseMore();const root=$('#view-more');if(!$('#premiumTools',root))root.insertAdjacentHTML('afterbegin',`<article id="premiumTools" class="card premium-tools"><div class="card-title-row"><div><h2>Finanzplan Premium-Zentrale</h2><p>Automatisierung, Berichte, Cloud, Banking und Vermögenswerte</p></div><span class="tag green">V3.1</span></div><div class="grid more-grid"><button class="more-tile" data-premium="autopilot"><span>✦</span><b>Finanz-Autopilot</b><small>Prognoseband, Anomalien, Maßnahmen</small></button><button class="more-tile" data-premium="monthly"><span>▤</span><b>Monatsbericht</b><small>Automatische Monatsanalyse</small></button><button class="more-tile" data-premium="year"><span>⌁</span><b>Jahresvergleich</b><small>Vorjahr & Inflation</small></button><button class="more-tile" data-premium="assets"><span>◇</span><b>Portfolio & Werte</b><small>Immobilien, Wertpapiere, Sonstiges</small></button><button class="more-tile" data-premium="cloud"><span>☁</span><b>Cloud & Multiuser</b><small>Supabase Login, Sync, Rollen, Konflikte</small></button><button class="more-tile" data-premium="n26"><span>€</span><b>N26 automatisch</b><small>PSD2 über Enable Banking</small></button><button class="more-tile" data-premium="ai"><span>✦</span><b>KI-Modus</b><small>Lokal, Geräte-KI oder Cloud-KI</small></button></div></article>`);$$('[data-premium]',root).forEach(b=>b.onclick=()=>({autopilot:openAutopilot,monthly:openMonthlyReport,year:openYearCompare,assets:openAssets,cloud:openCloudCenter,n26:openN26Center,ai:openAISettings}[b.dataset.premium]?.()))};

  function openMonthlyReport(){const d=new Date(selectedMonth.getFullYear(),selectedMonth.getMonth(),1),r=P().generateMonthlyReport(d,true);saveData('Monatsbericht aktualisiert');openModal(`Monatsbericht ${fmtMonth(d)}`,'Kennzahlen werden ausschließlich aus dem Finanzledger berechnet.',`<div class="four"><div class="kpi-small"><small>Einnahmen</small><strong class="money positive">${money(r.income)}</strong></div><div class="kpi-small"><small>Ausgaben</small><strong class="money negative">${money(r.expense)}</strong></div><div class="kpi-small"><small>Monatssaldo</small><strong class="money ${r.savings>=0?'positive':'negative'}">${money(r.savings)}</strong></div><div class="kpi-small"><small>Nettovermögen</small><strong class="money">${money(r.netWorth)}</strong></div></div><h3 style="margin-top:18px">Größte Kategorien</h3><div class="simple-list">${r.topCategories.map(x=>`<div class="list-row"><div><strong>${escapeHTML(x.name)}</strong></div><div class="money">${money(x.value)}</div></div>`).join('')}</div><h3 style="margin-top:18px">Größte Veränderungen zum Vormonat</h3><div class="simple-list">${r.changes.map(x=>`<div class="list-row"><div><strong>${escapeHTML(x.name)}</strong><small>Vormonat ${money(x.previous)}</small></div><div class="money ${x.diff<=0?'positive':'negative'}">${x.diff>=0?'+':''}${money(x.diff)}</div></div>`).join('')}</div><div class="modal-actions"><button id="downloadMonthReport" class="secondary-button">Bericht speichern</button><button class="secondary-button" data-cancel>Schließen</button></div>`,()=>{$('#downloadMonthReport').onclick=()=>{const txt=`Finanzplan Monatsbericht ${r.month}\nEinnahmen: ${r.income.toFixed(2)} EUR\nAusgaben: ${r.expense.toFixed(2)} EUR\nSaldo: ${r.savings.toFixed(2)} EUR\nNettovermögen: ${Number(r.netWorth).toFixed(2)} EUR\n\nTop-Kategorien:\n${r.topCategories.map(x=>`${x.name}: ${x.value.toFixed(2)} EUR`).join('\n')}`;download(`finanzplan-monatsbericht-${r.month}.txt`,txt,'text/plain;charset=utf-8')};$('[data-cancel]').onclick=closeModal})}
  function openYearCompare(){const y=selectedMonth.getFullYear(),r=P().annualCompare(y),infl=r.inflation;openModal(`Jahresvergleich ${y}`,'Optional inflationsbereinigt. Die Inflationsrate kannst du selbst setzen.',`<div class="four"><div class="kpi-small"><small>Ausgaben ${y}</small><strong class="money">${money(r.current.expense)}</strong></div><div class="kpi-small"><small>Ausgaben ${y-1}</small><strong class="money">${money(r.previous.expense)}</strong></div><div class="kpi-small"><small>Nominale Veränderung</small><strong class="money ${r.expenseChange<=0?'positive':'negative'}">${money(r.expenseChange)}</strong></div><div class="kpi-small"><small>Real nach Inflation</small><strong class="money ${r.realExpenseChange<=0?'positive':'negative'}">${money(r.realExpenseChange)}</strong></div></div><form id="inflationForm" style="margin-top:16px"><div class="field"><label>Inflationsrate ${y} in %</label><input name="rate" type="number" step="0.1" class="input" value="${infl}"></div><div class="modal-actions"><button class="secondary-button" data-cancel>Schließen</button><button class="primary-button">Rate speichern & neu berechnen</button></div></form>`,()=>{const f=$('#inflationForm');f.onsubmit=e=>{e.preventDefault();data.settings.inflationByYear[y]=num(new FormData(f).get('rate'));saveData('Inflationsrate gespeichert');openYearCompare()};$('[data-cancel]').onclick=closeModal})}

  function assetTypeLabel(t){return({property:'Immobilie',security:'Wertpapier',vehicle:'Fahrzeug',precious:'Edelmetall',other:'Sonstiges'})[t]||t}
  function openAssets(){const total=P().assetTotalAt(),alloc=P().assetAllocation();openModal('Portfolio & Vermögenswerte','Nicht-Kontowerte fließen in das Nettovermögen ein. Bewertungen bleiben nachvollziehbar historisiert.',`<div class="three"><div class="kpi-small"><small>Zusätzliche Vermögenswerte</small><strong class="money">${money(total)}</strong></div><div class="kpi-small"><small>Positionen</small><strong>${data.assets.length}</strong></div><div class="kpi-small"><small>Größte Klasse</small><strong>${alloc[0]?escapeHTML(assetTypeLabel(alloc[0].type)):'—'}</strong></div></div><div class="simple-list" style="margin-top:16px">${data.assets.length?data.assets.map(a=>`<div class="list-row"><div><strong>${escapeHTML(a.name)}</strong><small>${assetTypeLabel(a.type)}${a.type==='security'?` · ${num(a.quantity)} × ${money(a.currentPrice)}`:''}</small></div><div class="money"><b>${money(P().assetValueAt(a))}</b></div><button class="mini-btn" data-edit-asset="${a.id}">Bearbeiten</button></div>`).join(''):'<div class="empty">Noch keine zusätzlichen Vermögenswerte.</div>'}</div><div class="modal-actions"><button id="addAsset" class="primary-button">＋ Vermögenswert</button><button class="secondary-button" data-cancel>Schließen</button></div>`,()=>{$('#addAsset').onclick=()=>openAssetForm();$$('[data-edit-asset]').forEach(b=>b.onclick=()=>openAssetForm(data.assets.find(a=>a.id===b.dataset.editAsset)));$('[data-cancel]').onclick=closeModal})}
  function openAssetForm(a=null){const x=a||{name:'',type:'property',currentValue:0,quantity:0,currentPrice:0,note:'',active:true};openModal(a?'Vermögenswert bearbeiten':'Vermögenswert anlegen','Bei Wertpapieren: Menge × aktueller Preis. Sonst: aktueller Gesamtwert.',`<form id="assetForm"><div class="form-grid"><div class="field full"><label>Name</label><input name="name" class="input" value="${escapeHTML(x.name)}" required></div><div class="field"><label>Art</label><select name="type" class="select">${['property','security','vehicle','precious','other'].map(t=>`<option value="${t}" ${x.type===t?'selected':''}>${assetTypeLabel(t)}</option>`).join('')}</select></div><div class="field"><label>Gesamtwert</label><input name="currentValue" type="number" step="0.01" class="input" value="${num(x.currentValue)}"></div><div class="field"><label>Menge (Wertpapier)</label><input name="quantity" type="number" step="0.000001" class="input" value="${num(x.quantity)}"></div><div class="field"><label>Preis je Einheit</label><input name="currentPrice" type="number" step="0.0001" class="input" value="${num(x.currentPrice)}"></div><div class="field full"><label>Notiz</label><textarea name="note" class="textarea">${escapeHTML(x.note||'')}</textarea></div></div><div class="modal-actions">${a?'<button type="button" id="deleteAsset" class="danger-button">Löschen</button>':''}<button type="button" class="secondary-button" data-cancel>Abbrechen</button><button class="primary-button">Speichern</button></div></form>`,()=>{const f=$('#assetForm');f.onsubmit=e=>{e.preventDefault();const fd=new FormData(f);P().upsertAsset({...x,name:String(fd.get('name')),type:String(fd.get('type')),currentValue:num(fd.get('currentValue')),quantity:num(fd.get('quantity')),currentPrice:num(fd.get('currentPrice')),note:String(fd.get('note')||'')});closeModal();saveData('Vermögenswert gespeichert');openAssets()};$('#deleteAsset')?.addEventListener('click',()=>{if(confirm('Vermögenswert löschen?')){data.assets=data.assets.filter(v=>v.id!==x.id);closeModal();saveData('Vermögenswert gelöscht');openAssets()}});$('[data-cancel]').onclick=closeModal})}

  async function openCloudCenter(){const c=FinanzCloud.cloud(),s=await FinanzCloud.getSession();openModal('Cloud & Multiuser','Optional. Lokal funktioniert Finanzplan weiterhin vollständig ohne Cloud.',`<div id="cloudCenterBody"><form id="cloudConfig"><div class="form-grid"><div class="field full"><label>Supabase Projekt-URL</label><input name="url" class="input" value="${escapeHTML(c.url||'')}" placeholder="https://….supabase.co"></div><div class="field full"><label>Publishable / anon Key</label><input name="key" class="input" value="${escapeHTML(c.publishableKey||'')}" autocomplete="off"></div></div><div class="action-row"><button class="secondary-button">Konfiguration speichern</button></div></form>${s?`<div class="insight"><div class="insight-icon">✓</div><div><b>Cloud-Anmeldung aktiv</b><p>${escapeHTML(s.user?.email||'Angemeldet')} · Token wird verschlüsselt im Geräte-Speicher gehalten.</p></div></div><div class="action-row"><button id="cloudHouseholds" class="secondary-button">Haushalte</button><button id="cloudSync" class="primary-button">Jetzt synchronisieren</button><button id="cloudInvite" class="secondary-button">Mitglied einladen</button><button id="cloudLogout" class="secondary-button">Abmelden</button></div>`:`<form id="cloudLogin" style="margin-top:16px"><div class="form-grid"><div class="field"><label>E-Mail</label><input name="email" type="email" class="input" required></div><div class="field"><label>Passwort</label><input name="password" type="password" minlength="8" class="input" required></div></div><div class="action-row"><button type="button" id="cloudSignup" class="secondary-button">Konto anlegen</button><button class="primary-button">Anmelden</button></div></form>`}<div id="cloudStatus" style="margin-top:14px"></div></div><div class="modal-actions"><button class="secondary-button" data-cancel>Schließen</button></div>`,()=>{const config=$('#cloudConfig');config.onsubmit=e=>{e.preventDefault();const fd=new FormData(config);c.url=String(fd.get('url')||'').trim();c.publishableKey=String(fd.get('key')||'').trim();saveData('Cloud-Konfiguration gespeichert');toast('Cloud-Konfiguration gespeichert','success')};const login=$('#cloudLogin');if(login){login.onsubmit=async e=>{e.preventDefault();const fd=new FormData(login);try{await FinanzCloud.signIn(fd.get('email'),fd.get('password'));closeModal();openCloudCenter()}catch(err){toast(err.message,'error')}};$('#cloudSignup').onclick=async()=>{const fd=new FormData(login);try{const j=await FinanzCloud.signUp(fd.get('email'),fd.get('password'));toast(j.access_token?'Konto angelegt und angemeldet':'Konto angelegt. Bitte E-Mail bestätigen.','success');if(j.access_token){closeModal();openCloudCenter()}}catch(err){toast(err.message,'error')}}}$('#cloudSync')?.addEventListener('click',async()=>{try{$('#cloudStatus').textContent='Synchronisiere…';const r=await FinanzCloud.fullSync();$('#cloudStatus').innerHTML=`<div class="insight"><div class="insight-icon">☁</div><div><b>Sync abgeschlossen</b><p>${r.pulled} geladen · ${r.pushed} gesendet · ${r.conflicts.length} Konflikt(e) · ${r.documents.uploaded} Beleg(e)</p></div></div>`;if(r.conflicts.length)showCloudConflicts()}catch(err){toast(err.message,'error')}});$('#cloudHouseholds')?.addEventListener('click',openHouseholds);$('#cloudInvite')?.addEventListener('click',openCloudInvite);$('#cloudLogout')?.addEventListener('click',async()=>{await FinanzCloud.signOut();closeModal();openCloudCenter()});$('[data-cancel]').onclick=closeModal})}
  async function openHouseholds(){try{const rows=await FinanzCloud.listHouseholds();openModal('Cloud-Haushalte','Wähle einen Haushalt oder lege den ersten an.',`<div class="simple-list">${rows.length?rows.map(h=>`<div class="list-row"><div><strong>${escapeHTML(h.name)}</strong><small>${h.id}</small></div><button class="mini-btn" data-house="${h.id}">${FinanzCloud.cloud().householdId===h.id?'Aktiv':'Auswählen'}</button></div>`).join(''):'<div class="empty">Noch kein Cloud-Haushalt.</div>'}</div><div class="modal-actions"><button id="newCloudHouse" class="primary-button">＋ Haushalt</button><button class="secondary-button" data-cancel>Schließen</button></div>`,()=>{$$('#newCloudHouse');$('#newCloudHouse').onclick=async()=>{const name=prompt('Name des Haushalts:','Mein Haushalt');if(name){try{await FinanzCloud.createHousehold(name);openHouseholds()}catch(e){toast(e.message,'error')}}};$$('[data-house]').forEach(b=>b.onclick=()=>{FinanzCloud.selectHousehold(b.dataset.house);openHouseholds()});$('[data-cancel]').onclick=closeModal})}catch(e){toast(e.message,'error')}}
  function openCloudInvite(){openModal('Mitglied einladen','Erzeuge einen einmaligen Einladungscode. Das andere Konto nimmt ihn nach Anmeldung an.',`<form id="inviteForm"><div class="form-grid"><div class="field"><label>E-Mail</label><input name="email" type="email" class="input" required></div><div class="field"><label>Rolle</label><select name="role" class="select"><option value="adult">Erwachsener</option><option value="limited">Eingeschränkt</option><option value="admin">Admin</option></select></div></div><div id="inviteResult"></div><div class="modal-actions"><button class="secondary-button" data-cancel>Schließen</button><button class="primary-button">Code erzeugen</button></div></form>`,()=>{const f=$('#inviteForm');f.onsubmit=async e=>{e.preventDefault();const fd=new FormData(f);try{const r=await FinanzCloud.createInvite(fd.get('email'),fd.get('role'));$('#inviteResult').innerHTML=`<div class="insight"><div class="insight-icon">#</div><div><b>Einladungscode</b><p style="word-break:break-all">${escapeHTML(r.token)}</p></div></div>`}catch(err){toast(err.message,'error')}};$('[data-cancel]').onclick=closeModal})}
  function showCloudConflicts(){const m=data.sync?.cloudMeta,cs=m?.conflicts||[];if(!cs.length)return;openModal('Cloud-Konflikte','Beide Geräte haben denselben Datensatz verändert. Du entscheidest bewusst.',`<div class="simple-list">${cs.map(c=>`<div class="list-row"><div><strong>${escapeHTML(c.collection)} · ${escapeHTML(c.id)}</strong><small>${escapeHTML(c.reason)}</small></div><button class="mini-btn" data-local="${escapeHTML(c.key)}">Lokal</button><button class="mini-btn" data-remote="${escapeHTML(c.key)}">Cloud</button></div>`).join('')}</div><div class="modal-actions"><button class="secondary-button" data-cancel>Später</button></div>`,()=>{$$('[data-local]').forEach(b=>b.onclick=async()=>{await FinanzCloud.resolveConflict(b.dataset.local,'local');showCloudConflicts()});$$('[data-remote]').forEach(b=>b.onclick=async()=>{await FinanzCloud.resolveConflict(b.dataset.remote,'remote');showCloudConflicts()});$('[data-cancel]').onclick=closeModal})}

  function openN26Center(){const n=FinanzN26.conf();openModal('N26 automatisch synchronisieren','PSD2-Kontozugriff läuft über Enable Banking. Dein N26-Passwort wird niemals in Finanzplan gespeichert.',`<div class="three"><div class="kpi-small"><small>Status</small><strong>${n.sessionReady?'Verbunden':'Nicht verbunden'}</strong></div><div class="kpi-small"><small>Letzter Sync</small><strong>${n.lastSync?new Date(n.lastSync).toLocaleString('de-DE'):'—'}</strong></div><div class="kpi-small"><small>Letzter Saldo</small><strong class="money">${Number.isFinite(Number(n.lastBalance))?money(Number(n.lastBalance)):'—'}</strong></div></div><form id="n26Config" style="margin-top:16px"><div class="form-grid"><div class="field full"><label>Backend-URL</label><input name="backend" class="input" value="${escapeHTML(n.backendUrl||'')}" placeholder="https://finanzplan-api.onrender.com"></div><div class="field full"><label>N26 mit lokalem Konto verbinden</label><select name="account" class="select">${accountOptions(n.localAccountId)}</select></div></div><div class="action-row"><button class="secondary-button">Einstellungen speichern</button><button type="button" id="n26Connect" class="primary-button">${n.sessionReady?'N26 neu autorisieren':'N26 verbinden'}</button><button type="button" id="n26Sync" class="secondary-button" ${n.sessionReady?'':'disabled'}>Jetzt synchronisieren</button></div></form><div class="insight" style="margin-top:14px"><div class="insight-icon">🔐</div><div><b>Consent statt Bankpasswort</b><p>Die Freigabe erfolgt bei N26/Enable Banking und muss nach Ablauf des Bank-Consents erneuert werden.</p></div></div><div class="modal-actions"><button class="secondary-button" data-cancel>Schließen</button></div>`,()=>{const f=$('#n26Config');f.onsubmit=e=>{e.preventDefault();const fd=new FormData(f);n.backendUrl=String(fd.get('backend')||'').trim();n.localAccountId=String(fd.get('account')||'');saveData('N26-Einstellungen gespeichert')};$('#n26Connect').onclick=async()=>{try{const fd=new FormData(f);n.backendUrl=String(fd.get('backend')||'').trim();n.localAccountId=String(fd.get('account')||'');saveData('N26-Einstellungen gespeichert');await FinanzN26.start()}catch(err){toast(err.message,'error')}};$('#n26Sync').onclick=async()=>{try{toast('N26 wird synchronisiert…');const r=await FinanzN26.sync();toast(`${r.added} neue N26-Buchungen übernommen`,'success');openN26Center()}catch(err){toast(err.message,'error')}};$('[data-cancel]').onclick=closeModal})}

  function openAISettings(){const a=FinanzAI.aiCfg();openModal('KI-Modus','Rechnen bleibt immer lokal und deterministisch. KI erklärt ausschließlich bereits berechnete Kennzahlen.',`<form id="aiSettings"><div class="field"><label>Analysemodus</label><select name="mode" class="select"><option value="local" ${a.mode==='local'?'selected':''}>Lokaler Finanzanalyst – 0 €</option><option value="device" ${a.mode==='device'?'selected':''}>Geräte-KI / Browser Prompt API – 0 € wenn unterstützt</option><option value="cloud" ${a.mode==='cloud'?'selected':''}>Cloud-KI über eigenes Backend</option></select></div><div class="field" style="margin-top:12px"><label>Backend-URL für Cloud-KI</label><input name="backend" class="input" value="${escapeHTML(a.backendUrl||'')}" placeholder="https://finanzplan-api.onrender.com"></div><div id="deviceAIStatus" style="margin-top:12px"></div><div class="modal-actions"><button type="button" class="secondary-button" data-cancel>Schließen</button><button class="primary-button">Speichern</button></div></form>`,()=>{FinanzAI.deviceAvailability().then(v=>{$('#deviceAIStatus').innerHTML=`<small>Geräte-KI: ${escapeHTML(v)}</small>`});const f=$('#aiSettings');f.onsubmit=e=>{e.preventDefault();const fd=new FormData(f);a.mode=String(fd.get('mode'));a.backendUrl=String(fd.get('backend')||'').trim();saveData('KI-Modus gespeichert');closeModal()};$('[data-cancel]').onclick=closeModal})}

  const baseAssistant=renderAssistant;renderAssistant=function(){const root=$('#view-assistant'),a=FinanzAI.aiCfg(),score=financeScore();root.innerHTML=`<div class="assistant-shell"><article class="card chat"><div class="card-title-row"><div><h2>✦ Finanzassistent</h2><p>Lokale Berechnungen · KI darf nur erklären, nicht Kontostände erfinden</p></div><select id="assistantMode" class="select" style="max-width:190px"><option value="local" ${a.mode==='local'?'selected':''}>Lokal</option><option value="device" ${a.mode==='device'?'selected':''}>Geräte-KI</option><option value="cloud" ${a.mode==='cloud'?'selected':''}>Cloud-KI</option></select></div><div id="chatLog" class="chat-log">${data.assistantLog.map(m=>`<div class="bubble ${m.role==='user'?'user':'bot'}">${escapeHTML(m.text)}</div>`).join('')}</div><form id="chatForm" class="chat-form"><input id="chatInput" class="input" placeholder="z. B. Warum bleibt diesen Monat weniger übrig?" autocomplete="off"><button class="primary-button">Senden</button></form></article><aside class="grid"><article class="card"><div class="card-title-row"><div><h2>Finanzielle Stabilität</h2><p>Interner Haushaltsindikator</p></div></div><div style="display:grid;place-items:center"><div class="score-ring" style="--score:${score}"><strong>${score}</strong></div></div></article><article class="card"><h3>Schnellfragen</h3><div class="action-row" style="margin-top:12px"><button class="mini-btn ask-chip">Warum habe ich diesen Monat weniger übrig?</button><button class="mini-btn ask-chip">Wie viel kann ich noch ausgeben?</button><button class="mini-btn ask-chip">Wie sieht mein Jahresvergleich aus?</button><button class="mini-btn ask-chip">Welche Buchungen sind ungewöhnlich?</button></div></article></aside></div>`;$('#assistantMode').onchange=e=>{a.mode=e.target.value;saveData('KI-Modus geändert')};const ask=async q=>{q=String(q||'').trim();if(!q)return;data.assistantLog.push({role:'user',text:q});renderAssistant();try{const text=await FinanzAI.answer(q,a.mode);data.assistantLog.push({role:'bot',text:String(text)})}catch(err){data.assistantLog.push({role:'bot',text:`KI-Fehler: ${err.message}`})}saveData('Assistent aktualisiert');renderAssistant()};$('#chatForm').onsubmit=e=>{e.preventDefault();ask($('#chatInput').value)};$$('.ask-chip',root).forEach(b=>b.onclick=()=>ask(b.textContent));const log=$('#chatLog');log.scrollTop=log.scrollHeight};

  if(typeof renderWealth==='function'){const baseWealth=renderWealth;renderWealth=function(){baseWealth();const root=$('#view-wealth');if(!$('#assetWealthCard',root))root.insertAdjacentHTML('beforeend',`<article id="assetWealthCard" class="card" style="margin-top:16px"><div class="card-title-row"><div><h2>Zusätzliche Vermögenswerte</h2><p>Immobilien, Wertpapiere, Fahrzeuge, Edelmetalle</p></div><button id="wealthAssets" class="secondary-button">Verwalten</button></div><div class="goal-value money">${money(P().assetTotalAt())}</div></article>`);$('#wealthAssets',root)?.addEventListener('click',openAssets)}}

  function setupColdPasskeyModal(){openModal('Cold-Start-Passkey einrichten','Der Tresorschlüssel wird mit einer WebAuthn-PRF des Geräte-Passkeys umschlossen. Dein PIN wird nicht gespeichert.',`<form id="coldPasskeyForm"><div class="field"><label>Aktuellen Tresor-PIN bestätigen</label><input name="pin" type="password" class="input" minlength="6" required></div><div class="modal-actions"><button type="button" class="secondary-button" data-cancel>Abbrechen</button><button class="primary-button">Passkey einrichten</button></div></form>`,()=>{const f=$('#coldPasskeyForm');f.onsubmit=async e=>{e.preventDefault();try{await FinanzPasskey.setup(String(new FormData(f).get('pin')));closeModal();toast('Cold-Start-Passkey eingerichtet','success')}catch(err){toast(err.message,'error')}};$('[data-cancel]').onclick=closeModal})}
  const baseSettings=renderSettings;renderSettings=function(){baseSettings();const root=$('#view-settings');if(!$('#premiumSecuritySettings',root))root.insertAdjacentHTML('beforeend',`<article id="premiumSecuritySettings" class="card" style="margin-top:16px"><div class="card-title-row"><div><h2>Premium-Sicherheit & Integrationen</h2><p>Passkey-Cold-Start, Cloud, N26 und KI</p></div><span class="tag green">V3.1</span></div><div class="setting-row"><div><b>Cold-Start-Passkey</b><small style="display:block;color:var(--muted)">${FinanzPasskey.wrapper()?'Eingerichtet – Tresor kann nach Neustart per Passkey geöffnet werden':'Optional für den verschlüsselten Tresor'}</small></div><button id="setupColdPasskey" class="secondary-button">${FinanzPasskey.wrapper()?'Neu einrichten':'Einrichten'}</button></div><div class="setting-row"><div><b>Cloud & Multiuser</b><small style="display:block;color:var(--muted)">${data.integrations?.cloud?.householdId?'Cloud-Haushalt gewählt':'Optional, Local-first bleibt aktiv'}</small></div><button id="settingsCloud" class="secondary-button">Öffnen</button></div><div class="setting-row"><div><b>N26 PSD2</b><small style="display:block;color:var(--muted)">${data.integrations?.n26?.sessionReady?'Verbunden':'Noch nicht autorisiert'}</small></div><button id="settingsN26" class="secondary-button">Öffnen</button></div><div class="setting-row"><div><b>KI-Analyse</b><small style="display:block;color:var(--muted)">Modus: ${escapeHTML(FinanzAI.aiCfg().mode)}</small></div><button id="settingsAI" class="secondary-button">Öffnen</button></div></article>`);$('#setupColdPasskey',root)?.addEventListener('click',()=>{if(!data.settings?.vault)return toast('Bitte zuerst den Finanz-Tresor aktivieren','error');setupColdPasskeyModal()});$('#settingsCloud',root)?.addEventListener('click',openCloudCenter);$('#settingsN26',root)?.addEventListener('click',openN26Center);$('#settingsAI',root)?.addEventListener('click',openAISettings)};

  const baseLock=typeof showLockIfNeeded==='function'?showLockIfNeeded:null;if(baseLock)showLockIfNeeded=async function(){if(vaultExists()&&!window.__vaultKey&&FinanzPasskey.wrapper()){ $('#modalClose').classList.add('hidden');openModal('Finanzplan-Tresor entsperren','Cold-Start-Passkey ist eingerichtet. Alternativ funktioniert weiterhin dein Tresor-PIN.',`<form id="v31VaultUnlock"><div class="field"><label>Tresor-PIN</label><input name="pin" type="password" class="input"></div><button type="button" id="coldPasskeyUnlock" class="secondary-button" style="width:100%;margin-top:10px">Mit Passkey / Biometrie entsperren</button><div class="modal-actions"><button class="primary-button">Mit PIN entsperren</button></div></form>`,()=>{const f=$('#v31VaultUnlock');f.onsubmit=async e=>{e.preventDefault();try{await unlockVault(String(new FormData(f).get('pin')));$('#modalClose').classList.remove('hidden');closeModal();toast('Tresor entsperrt','success')}catch{toast('PIN falsch oder Tresordaten beschädigt','error')}};$('#coldPasskeyUnlock').onclick=async()=>{try{await FinanzPasskey.unlock();$('#modalClose').classList.remove('hidden');closeModal();toast('Tresor per Passkey entsperrt','success')}catch(err){toast(err.message,'error')}}});return }return baseLock()};

  window.FinanzPremiumUI={openAutopilot,openMonthlyReport,openYearCompare,openAssets,openCloudCenter,openN26Center,openAISettings,showCloudConflicts};
})();
