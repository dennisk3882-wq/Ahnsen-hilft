'use strict';
function openMoreTool(key){
  if(key==='scenario')return openScenario();if(key==='purchase')return openPurchase();if(key==='documents')return openDocuments();if(key==='import')return openImport();if(key==='export')return openExport();if(key==='bank')return openBank();if(key==='members')return openMembers();if(key==='categories')return openCategories();if(key==='backup')return openBackup();
}
function openScenario(){const s=monthSummary();openModal('Szenario-Rechner','Teste Veränderungen, ohne deine echten Daten zu verändern.',`<form id="scenarioForm"><div class="form-grid"><div class="field"><label>Basis-Einnahmen</label><input name="income" type="number" step="0.01" class="input" value="${s.totalExpectedIncome}"></div><div class="field"><label>Basis-Ausgaben</label><input name="expense" type="number" step="0.01" class="input" value="${s.totalExpectedExpense}"></div><div class="field"><label>Gehalt verändert sich um</label><input name="incomeDelta" type="number" step="0.01" class="input" value="0"></div><div class="field"><label>Fixkosten verändern sich um</label><input name="expenseDelta" type="number" step="0.01" class="input" value="0"></div><div class="field"><label>Zusätzliche Rate</label><input name="rate" type="number" step="0.01" class="input" value="0"></div></div><div id="scenarioResult" class="scenario-result"></div><div class="modal-actions"><button type="button" class="primary-button" data-cancel>Schließen</button></div></form>`,()=>{const f=$('#scenarioForm'),calc=()=>{const fd=new FormData(f),inc=num(formVal(fd,'income'))+num(formVal(fd,'incomeDelta')),exp=num(formVal(fd,'expense'))+num(formVal(fd,'expenseDelta'))+num(formVal(fd,'rate')),left=inc-exp;$('#scenarioResult').innerHTML=`<div class="three"><div><small>Neue Einnahmen</small><strong class="positive money">${money(inc)}</strong></div><div><small>Neue Ausgaben</small><strong class="negative money">${money(exp)}</strong></div><div><small>Neuer Überschuss</small><strong class="${left>=0?'positive':'negative'} money">${money(left)}</strong></div></div><p style="color:var(--muted);font-size:11px">Sparquote in diesem Szenario: ${pct(inc?left/inc*100:0)}.</p>`};f.oninput=calc;calc();$('[data-cancel]',f).onclick=closeModal})}
function openPurchase(){openModal('Anschaffungs-Rechner','Berechnet Sparrate und Auswirkung auf deinen aktuellen Cashflow.',`<form id="purchaseForm"><div class="form-grid"><div class="field"><label>Anschaffung</label><input name="name" class="input" value="Neue Anschaffung"></div><div class="field"><label>Preis</label><input name="cost" type="number" step="0.01" class="input" value="5000"></div><div class="field"><label>Bereits vorhanden</label><input name="saved" type="number" step="0.01" class="input" value="1000"></div><div class="field"><label>Ziel in Monaten</label><input name="months" type="number" min="1" class="input" value="12"></div></div><div id="purchaseResult" class="scenario-result"></div><div class="modal-actions"><button type="button" class="primary-button" data-cancel>Schließen</button></div></form>`,()=>{const f=$('#purchaseForm'),calc=()=>{const fd=new FormData(f),cost=num(formVal(fd,'cost')),saved=num(formVal(fd,'saved')),months=Math.max(1,num(formVal(fd,'months'))),need=Math.max(0,cost-saved),rate=need/months,free=monthSummary().forecast,after=free-rate;$('#purchaseResult').innerHTML=`<b>${escapeHTML(formVal(fd,'name'))}</b><div class="three" style="margin-top:10px"><div><small>Noch benötigt</small><strong class="money">${money(need)}</strong></div><div><small>Nötige Sparrate</small><strong class="money">${money(rate)}/Mon.</strong></div><div><small>Monatsprognose danach</small><strong class="${after>=0?'positive':'negative'} money">${money(after)}</strong></div></div><p style="color:var(--muted);font-size:11px">${after>=0?'Die Rate passt rechnerisch in die aktuelle Monatsprognose.':'Die gewünschte Sparrate würde die aktuelle Monatsprognose ins Minus drücken.'}</p>`};f.oninput=calc;calc();$('[data-cancel]',f).onclick=closeModal})}

let dbPromise=null;