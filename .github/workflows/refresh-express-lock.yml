name: Refresh Express Lockfile

on:
  push:
    branches:
      - stabilitaet-phase10-v101
    paths:
      - '.github/workflows/refresh-express-lock.yml'

permissions:
  contents: write

jobs:
  refresh-lock:
    runs-on: ubuntu-latest
    steps:
      - name: Repository auschecken
        uses: actions/checkout@v4
        with:
          ref: stabilitaet-phase10-v101
          fetch-depth: 0

      - name: Node.js einrichten
        uses: actions/setup-node@v4
        with:
          node-version: '24'
          cache: npm
          cache-dependency-path: quiz-app/package-lock.json

      - name: Sichere Express-Version und Lockfile erzeugen
        working-directory: quiz-app
        run: npm install express@4.22.2 --save-exact --package-lock-only --ignore-scripts

      - name: Aktualisierung committen
        run: |
          git config user.name "quiztime-ci"
          git config user.email "quiztime-ci@users.noreply.github.com"
          git add quiz-app/package.json quiz-app/package-lock.json
          git diff --cached --quiet && exit 0
          git commit -m "Express-Sicherheitsupdate auf 4.22.2"
          git push origin HEAD:stabilitaet-phase10-v101
