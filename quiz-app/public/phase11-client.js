'use strict';

(() => {
  if (window.__quiztimePhase11ClientInstalled) return;
  window.__quiztimePhase11ClientInstalled = true;
  for (const href of ['/phase12-13.css']) { if (!document.querySelector(`link[href="${href}"]`)) { const link = document.createElement('link'); link.rel = 'stylesheet'; link.href = href; document.head.appendChild(link); } }
  if (!document.querySelector('script[src="/phase12-13-client.js"]')) { const script = document.createElement('script'); script.src = '/phase12-13-client.js'; script.defer = true; document.body.appendChild(script); }
  document.querySelector('#topModeBadge')?.replaceChildren(document.createTextNode('Version 13.0'));
  const banner = document.querySelector('.phase-banner');
  if (banner) { const icon = banner.querySelector('.phase-banner-icon'); const strong = banner.querySelector('strong'); const detail = banner.querySelector('strong + span'); const action = banner.querySelector('a'); if (icon) icon.textContent = '13'; if (strong) strong.textContent = 'QuizTime 13: Qualität, Datenschutz und Fortschritt'; if (detail) detail.textContent = 'Fragenmeldungen, Release-Prüfung, rechtliche Seiten, Serien, Wochenziele und persönliche Empfehlungen sind integriert.'; if (action) { action.href = '/progress'; action.textContent = 'Fortschritt öffnen'; } }
  async function json(url, options = {}) { const response = await fetch(url, { ...options, headers: { 'Content-Type': 'application/json', ...(options.headers || {}) } }); const data = await response.json().catch(() => ({})); if (!response.ok) throw Object.assign(new Error(data.error || 'Anfrage fehlgeschlagen.'), { status: response.status }); return data; }
  function addOnboardingBanner(state) { if (state.complete || state.dismissed || document.querySelector('#phase11OnboardingBanner') || location.pathname === '/welcome') return; const anchor = document.querySelector('.app-topbar, header'); if (!anchor) return; const banner = document.createElement('aside'); banner.id = 'phase11OnboardingBanner'; banner.className = 'phase11-onboarding-banner'; banner.innerHTML = `<div><strong>QuizTime-Einführung: ${state.completedCount}/${state.total}</strong><span>Noch ${state.total - state.completedCount} Schritte bis zur Abschlussbelohnung.</span></div><a class="btn primary small" href="/welcome">Fortsetzen</a><button type="button" aria-label="Einführung ausblenden">×</button>`; anchor.insertAdjacentElement('afterend', banner); banner.querySelector('button').onclick = async () => { banner.remove(); await json('/api/platform/phase11/onboarding/dismiss', { method: 'POST', body: JSON.stringify({ dismissed: true }) }).catch(() => {}); }; }
  function addNotices(data) { const notices = (data.notices || []).filter(item => !item.acknowledged_at); if (!notices.length || document.querySelector('#phase11Notice')) return; const notice = notices[0]; const panel = document.createElement('div'); panel.id = 'phase11Notice'; panel.className = `phase11-player-notice ${notice.notice_type || 'warning'}`; panel.innerHTML = '<section><span class="app-kicker">Hinweis von QuizTime</span><h2></h2><p></p><button class="btn primary">Gelesen</button></section>'; panel.querySelector('h2').textContent = notice.title; panel.querySelector('p').textContent = notice.body; document.body.appendChild(panel); panel.querySelector('button').onclick = async () => { await json(`/api/platform/phase11/notices/${encodeURIComponent(notice.id)}/acknowledge`, { method: 'POST', body: '{}' }).catch(() => {}); panel.remove(); }; }
  (async () => { if (location.pathname === '/arena') json('/api/platform/phase11/onboarding/steps/arena', { method: 'POST', body: '{}' }).catch(() => {}); try { const [onboarding, notices] = await Promise.all([json('/api/platform/phase11/onboarding'), json('/api/platform/phase11/notices').catch(() => ({ notices: [] }))]); addOnboardingBanner(onboarding); addNotices(notices); } catch (error) { if (![401,403,428].includes(error.status)) console.warn(error.message); } })();
})();
